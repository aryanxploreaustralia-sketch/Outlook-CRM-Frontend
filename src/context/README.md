# src/context

React Context providers for cross-cutting client state.
Authentication and theme providers will live here in later phases.
Mount them inside `src/App.jsx`, which is the single composition point.

