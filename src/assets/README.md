# src/assets

Static assets imported by components (SVG, images, fonts).
Files here are processed and fingerprinted by Vite at build time.
Assets that must keep a stable URL belong in `public/` instead.

