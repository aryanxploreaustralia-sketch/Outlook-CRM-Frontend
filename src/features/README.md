# src/features

Feature-sliced modules. Each subfolder owns one CRM capability and
colocates its components, hooks and services (for example `features/campaigns/`).
Anything shared by two or more features is promoted to the top-level folders.

